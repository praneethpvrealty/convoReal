/** Max properties an account can star as Contacts-page interest
 *  filters. Kept small on purpose — the chips live below the
 *  Contacts search bar and must stay scannable. Enforced in the
 *  properties PUT handler and mirrored in the Inventory UI. */
export const STARRED_PROPERTY_CAP = 6;
